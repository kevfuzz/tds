# External-repo teams consume the publishable @rwp2/* libs from the internal
# npm registry (02 §2). Scope is mapped here; the token is injected by CI.
@rwp2:registry=https://npm.rev.internal/
# registry=https://registry.npmjs.org/   # default public registry for everything else
